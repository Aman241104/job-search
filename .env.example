GEMINI_API_KEY=your_gemini_api_key_here

# Optional, LOCAL DEV ONLY — never set these in Render/Vercel env vars.
# Uses your local `claude login` session (Pro/Max subscription usage, not API
# billing) for CV/cover-letter generation only. Falls back to Gemini automatically
# if unset, or if the claude CLI isn't installed/logged in.
# AI_PROVIDER=claude_code
# CLAUDE_CODE_BIN=claude

SMTP_EMAIL=patelaman0241@gmail.com
SMTP_PASSWORD=your_gmail_app_password_here
ADZUNA_APP_ID=your_adzuna_app_id
ADZUNA_APP_KEY=your_adzuna_app_key
