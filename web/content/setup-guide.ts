export const setupGuideMd = "# Setting up your own job-serach instance\n\nThis guide walks through `scripts/setup.py`, the one command that gets your own private copy of the app running. It's entirely free \u2014 no credit card needed unless you choose to deploy it to the cloud at the very end.\n\nEverything you enter gets saved into a `.env` file that only you can see (it's git-ignored \u2014 it never gets committed or shared).\n\n---\n\n## 0. Before you start\n\nInstall these two things if you don't have them:\n\n- **Python 3.11+** and **Node.js 18+**\n- **ffmpeg** \u2014 needed for one feature (YouTube playlist transcription fallback). Install with:\n  - Mac: `brew install ffmpeg`\n  - Ubuntu/Debian: `sudo apt install ffmpeg`\n  - Windows: [download here](https://ffmpeg.org/download.html) and add it to PATH\n\n## 1. Clone and run the setup script\n\n```bash\ngit clone <the-repo-url>\ncd job-serach\npython scripts/setup.py\n```\n\nThe script is **interactive** \u2014 it asks you questions one at a time and explains exactly where to get each thing. You can press **Enter to skip** any optional item. Nothing bad happens if you skip something; that feature just won't work until you add the key later (you can always re-run the script \u2014 it's safe to run more than once).\n\nIt has 7 short sections:\n\n### 1/7 \u2014 Database (required)\n\nYou need somewhere to store your job data. The script points you to **Supabase** (a free hosted Postgres database):\n\n1. Go to [supabase.com](https://supabase.com) and create a free project\n2. In your project: **Settings \u2192 Database \u2192 Connection string \u2192 URI** (make sure it says \"pooled\", port `6543`)\n3. Paste that connection string when the script asks\n\nYou don't need to create any tables yourself \u2014 the app creates them automatically the first time it runs.\n\n*(If you skip this, the app falls back to a local SQLite file instead \u2014 totally fine for solo/local use, you just won't be able to sync data between your laptop and a deployed version.)*\n\n### 2/7 \u2014 AI provider (required)\n\nThis powers everything AI-related: scoring jobs against your resume, writing cover letters, the interview trainer, and the \"ask questions about a YouTube playlist\" feature.\n\n1. Go to [build.nvidia.com](https://build.nvidia.com), sign up (free, no card)\n2. Open any model page, click **\"Get API Key\"**\n3. Paste that key when asked\n\nOptionally also grab a free [Gemini key](https://aistudio.google.com/apikey) \u2014 it's used as an automatic backup if NVIDIA ever rate-limits you.\n\n### 3/7 \u2014 Email (optional, needed for the \"apply via email\" feature)\n\nUses your Gmail to send job applications on your behalf. **Do not use your real Gmail password** \u2014 generate an \"App Password\" instead:\n\n1. Turn on 2-Factor Authentication on your Google account (if not already on)\n2. Go to [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)\n3. Generate one, paste it into the script\n\n### 4/7 \u2014 Extra job sources (optional, all free)\n\nMore places to pull job listings from. The app works fine with zero of these \u2014 they just widen your search:\n\n- **Adzuna**: free key at [developer.adzuna.com/signup](https://developer.adzuna.com/signup)\n- **Jooble**: free key at [jooble.org/api/about](https://jooble.org/api/about)\n\n### 5/7 \u2014 Telegram bot (optional \u2014 job alerts on your phone)\n\nLets the app message you when it finds a good match, and you can reply to mark yourself as \"applied.\" If you want this:\n\n1. Message **@BotFather** on Telegram, send `/newbot`, follow its prompts\n2. Message your new bot anything once\n3. Visit `https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates` in a browser \u2014 your chat ID is in that JSON response\n\nThe script generates a random secret for you automatically here \u2014 nothing to do.\n\n### 6/7 \u2014 Cloudinary (optional \u2014 lets you upload PDF books/notes in the Learning tab)\n\n1. Free account at [cloudinary.com](https://cloudinary.com)\n2. Your Cloud Name / API Key / API Secret are all on your dashboard homepage\n3. **Important:** make sure the API key has \"Master Admin\" (or at least \"create\") permission under Settings \u2192 API Keys, or file uploads will fail with a confusing 403 error even though the key looks like it \"works.\"\n\n### 7/7 \u2014 Deploy (optional)\n\nAt this point the script has already written your `.env` file. It then asks if you want to deploy to Google Cloud Run right now. **Say no for now** \u2014 run it locally first to make sure everything works, then deploy later if you want a permanent hosted version. (Deploying needs the `gcloud` CLI and a GCP project with billing enabled \u2014 still free under the usage tier, just needs a card on file.)\n\n---\n\n## 2. Run it locally\n\nOnce the script finishes, start the backend:\n\n```bash\npip install -r requirements.txt\nuvicorn app:app --reload\n```\n\nThen, in a second terminal, start the frontend:\n\n```bash\ncd web\nnpm install\necho \"NEXT_PUBLIC_API_URL=http://localhost:8000\" > .env.local\nnpm run dev\n```\n\nOpen **http://localhost:3000** \u2014 that's your dashboard.\n\n---\n\n## 3. If something's wrong later\n\n- Re-run `python scripts/setup.py` anytime \u2014 it's safe, and if `.env` already exists it'll ask before overwriting (you can also say no and it'll just print the values so you can merge them by hand).\n- If a key ever breaks (expires, project gets deleted, etc.), just edit `.env` directly \u2014 it's a plain text file, one `KEY=value` per line.\n";
