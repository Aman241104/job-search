export const jobFinderMd = "# Job Finder \u2014 how jobs get found and scored\n\n`agents/job_finder.py`, `JobFinderAgent`. This is what `python main.py find` and the dashboard's \"Find New Jobs\" action both run.\n\n---\n\n## The 14 sources\n\n```mermaid\nflowchart TD\n    F[\"find_jobs()\"] --> S1[\"Internshala\\n(scraped, 7 searches)\"]\n    F --> S2[\"Jobicy\\n(free API)\"]\n    F --> S3[\"Adzuna\\n(optional, needs key)\"]\n    F --> S4[\"Jooble\\n(optional, needs key)\"]\n    F --> S5[\"Careerjet\\n(optional, needs key)\"]\n    F --> S6[\"WeWorkRemotely\\n(RSS)\"]\n    F --> S7[\"Arbeitnow\\n(free API)\"]\n    F --> S8[\"LinkedIn\\n(guest API, no auth)\"]\n    F --> S9[\"Remotive\\n(free API)\"]\n    F --> S10[\"RemoteOK\\n(free API)\"]\n    F --> S11[\"Remote.co\\n(RSS)\"]\n    F --> S12[\"The Muse\\n(free API)\"]\n    F --> S13[\"Himalayas\\n(free API)\"]\n    F --> S14[\"HN Who's Hiring\\n(monthly thread)\"]\n    S1 & S2 & S3 & S4 & S5 & S6 & S7 & S8 & S9 & S10 & S11 & S12 & S13 & S14 --> DEDUPE[\"Dedupe by URL\\n+ filter senior titles\\n+ cap 3 jobs/company\"]\n    DEDUPE --> SCORE[\"score_jobs():\\nkeyword scan, then AI\\nfor uncertain ones\"]\n    SCORE --> SAVE[\"Save to data/found_jobs.json\"]\n```\n\nEach source has its own `_fetch_*`/`_scrape_*` method, each wrapped in its own try/except that returns `[]` on failure and prints a yellow warning \u2014 one source going down (rate-limited, redesigned HTML, whatever) never stops the other 13. Three flavors of source:\n\n- **Free public APIs** (Jobicy, Arbeitnow, Remotive, RemoteOK, The Muse, Himalayas) \u2014 just JSON over HTTP, no key needed, filtered client-side by title/tag keywords.\n- **Key-gated APIs** (Adzuna, Jooble, Careerjet) \u2014 official, no bot-detection surface, but only run if the corresponding `_APP_ID`/`_API_KEY` env var is actually set (`if not JOOBLE_API_KEY: return []`).\n- **Scraped HTML/RSS** (Internshala, LinkedIn's guest search endpoint, WeWorkRemotely, Remote.co, HN) \u2014 parsed with BeautifulSoup, more fragile, most likely to need maintenance if a site changes its markup.\n\n### The LinkedIn description gap\n\nLinkedIn's guest search API never includes the job description \u2014 only the individual job page has it, and that page is more aggressively bot-protected. `_enrich_linkedin_descriptions()` exists to fetch it via Crawl4AI (headless browser) but is **deliberately not called** by default:\n\n```python\n# NOT calling _enrich_linkedin_descriptions() here by default \u2014 live\n# testing 2026-07-04 showed repeated use escalates LinkedIn's\n# anti-bot system (explicit [ANTIBOT] errors, success rate dropped\n# from 15/15 to 4/43 across two sessions minutes apart).\n```\n\nThe risk wasn't the enrichment failing gracefully (it does \u2014 bad text just falls back to an empty description) \u2014 it was that hammering LinkedIn's individual job pages could get the *entire IP* flagged broadly enough to break the guest search API too, which already works reliably. A real example of \"the safe failure mode isn't the risk \u2014 the blast radius on a shared resource is.\"\n\n### Naukri is dead weight\n\n`_scrape_naukri()` still exists in the file but is never called from `find_jobs()`. Naukri's anti-bot measures made it unreliable enough that it was cut from the active pipeline, but the code was left in rather than deleted \u2014 it's a documented dead end (see `README.md`'s ToS section), kept as reference rather than resurrected.\n\n---\n\n## Scoring \u2014 two-phase, keyword first, AI only for the uncertain middle\n\n```python\ndef score_jobs(self, jobs):\n    results, uncertain, uncertain_idx = [], [], []\n    for i, job in enumerate(jobs):\n        score, reason = self.keyword_score(job)\n        results.append({'score': score, 'reason': reason})\n        if 35 <= score <= 65:\n            uncertain.append(job)\n            uncertain_idx.append(i)\n\n    if uncertain:\n        for i, (idx, job) in enumerate(zip(uncertain_idx, uncertain)):\n            results[idx] = score_job_single(job, self.PROFILE)\n            if i < len(uncertain) - 1:\n                time.sleep(1.5)\n    return results\n```\n\n**Phase 1 \u2014 `keyword_score()`**, pure Python, zero API calls, runs on every job:\n\n- Starts at a base of 30\n- `SKILL_WEIGHTS` \u2014 a dict of ~25 skill keywords (\"react\": 18, \"next.js\": 15, \"typescript\": 10, ...), summed and capped at +45\n- `PENALTY_SKILLS` \u2014 wrong-domain keywords (\"ruby\": -15, \"data science\": -20, \"devops\": -15, ...)\n- `EXPERIENCE_PATTERNS` \u2014 regexes matched against the text (\"fresher\" \u2192 +20, \"5+ years\" \u2192 -25), first match wins\n- Location bonus: Remote \u2192 +20, Gujarat cities \u2192 +15, far Indian cities (Bangalore/Mumbai/Pune/etc.) \u2192 -12\n- Salary parsing (Indian LPA format and plain \u20b9 ranges) \u2192 up to +15\n\n**Phase 2 \u2014 AI re-scoring**, only for jobs that landed in the 35\u201365 \"uncertain\" band after phase 1 (i.e., the keyword heuristic genuinely couldn't tell if it's a good match). This is deliberate \u2014 most jobs are confidently good or confidently bad from keywords alone; AI calls are reserved for the genuinely ambiguous middle, keeping API cost proportional to actual uncertainty rather than scoring every job with AI regardless.\n\n`score_job_single()` (in `claude_client.py`) sends one job at a time to NVIDIA/Gemini with an explicit rubric (80-100 for strong React/Next.js fresher-friendly remote-or-Gujarat matches, penalties for far-city onsite, bonus for remote) and parses a `{\"score\": int, \"reason\": str}` JSON response.\n\n---\n\n## Dedup + final filter\n\n```python\nfor j in all_jobs:\n    if not url or url in seen_urls or url in self.existing_urls: continue\n    if self._is_senior(j.get(\"title\", \"\")): continue\n    if company_count.get(company, 0) >= 3: continue\n    ...\n```\n\nThree gates, in order: skip if the URL was already seen this run *or* already in the saved dataset (`existing_urls`, loaded once at agent construction from `found_jobs.json`); skip if the title contains a senior-level word (`SENIOR_WORDS` \u2014 \"staff\", \"lead\", \"principal\", \"director\", etc.); cap at 3 jobs per company so one prolific poster doesn't crowd out everything else.\n\nStorage is a flat JSON file (`data/found_jobs.json`), not the SQLite/Postgres database \u2014 `orchestrator.py`'s `daily_run()` is what actually copies scored jobs into `TrackerAgent`'s real database via `tracker.add_job()`, one call per job. `job_finder.py` itself never touches the DB.\n";
