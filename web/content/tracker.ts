export const trackerMd = "# Tracker \u2014 the database layer\n\n`agents/tracker.py`, `TrackerAgent`. Every other agent and every `app.py` route that reads or writes persisted data goes through this file. Covered separately from the architecture doc's brief mention because there's real design worth understanding here, not just \"it's the DB layer.\"\n\n---\n\n## 17 tables, one file\n\n```\njobs                      \u2014 every scraped job (title, company, score, status, ...)\napplications              \u2014 status-change history (found -> applied -> interviewing -> offer/rejected/ghosted)\ntraining_sessions         \u2014 interview trainer results, persisted (used to be JSON-only)\nblacklisted_companies     \u2014 companies you've flagged to stop seeing\ninterview_rounds          \u2014 per-job interview round tracking\nlearning_items            \u2014 the Learning tab's Skills & Courses checklist\nlearning_topics           \u2014 AI-generated coverage checklist per learning item\nlearning_books            \u2014 uploaded PDF books\nlearning_book_pages       \u2014 extracted text per page (for the book tutor chat)\ntelegram_alerts           \u2014 sent Telegram job alerts (idempotency + reply matching)\nstory_bank                \u2014 STAR+Reflection interview stories\nsettings                  \u2014 misc key/value app settings\nbatches / batch_items     \u2014 batch-apply runs and their per-job outcomes\nlearning_playlists        \u2014 YouTube playlist study RAG (see that doc)\nlearning_videos           \u2014   \u21b3 per-video transcript/notes/status\nlearning_video_chunks     \u2014   \u21b3 chunked+embedded transcript pieces\n```\n\nAll created idempotently in `_init_db()` via `CREATE TABLE IF NOT EXISTS` \u2014 runs on every `TrackerAgent()` construction, so there's no separate migration step; the schema just self-heals to whatever the code expects on next startup. New columns added later use `ALTER TABLE ... ADD COLUMN` wrapped in a try/except that silently no-ops if the column already exists (SQLite has no `ADD COLUMN IF NOT EXISTS`).\n\n---\n\n## The dual SQLite/Postgres trick\n\nThis is the most interesting piece of engineering in the file. Local dev needs zero setup; production needs a real concurrent-safe database. Rather than writing two versions of every query, `tracker.py` makes one Postgres connection *pretend to be* a SQLite connection:\n\n```python\nDATABASE_URL = os.getenv(\"DATABASE_URL\", \"\")\n_pg_pool = None\nif DATABASE_URL:\n    _pg_pool = psycopg2.pool.ThreadedConnectionPool(1, 10, DATABASE_URL)\n\ndef _get_conn(self):\n    if _pg_pool:\n        return _PgConnWrapper(_pg_pool)\n    return sqlite3.connect(self.db_path)\n```\n\n`_PgConnWrapper` implements exactly the subset of `sqlite3.Connection`'s interface the rest of the file actually uses:\n\n```python\nclass _PgConnWrapper:\n    def __init__(self, pool):\n        self._pool = pool\n        self._conn = pool.getconn()\n        self.row_factory = None\n\n    def execute(self, sql, params=()):\n        cursor = self._conn.cursor()\n        cursor.execute(sql.replace(\"?\", \"%s\"), params)\n        return _DictCursorProxy(cursor) if self.row_factory else cursor\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        if exc_type is None: self._conn.commit()\n        else: self._conn.rollback()\n        self._pool.putconn(self._conn)  # returns to pool, doesn't close\n```\n\nTwo translations happen transparently:\n1. **Placeholder syntax** \u2014 SQLite uses `?`, Postgres uses `%s`. Every query in this file is written with `?` (SQLite's style); `.execute()` does a blind `.replace(\"?\", \"%s\")` before handing it to psycopg2. (This works because none of the queries here have a literal `?` character inside a string value \u2014 if one ever did, this would need a smarter replace.)\n2. **Row shape** \u2014 `sqlite3.Row` supports both dict-style (`row[\"title\"]`) and positional (`row[0]`) access; some methods in this file rely on positional access. `_DictCursorProxy` wraps a plain psycopg2 cursor and converts `fetchone()`/`fetchall()` into plain dicts (via `zip(column_names, row)`), matching what `dict(sqlite3.Row(...))` would produce, only activated when `row_factory` is set \u2014 same opt-in pattern as `sqlite3.Connection.row_factory`.\n\nThe result: every one of the ~50 query methods below `_init_db()` is written once, tested against SQLite locally, and works unmodified against Postgres in production. Set `DATABASE_URL` \u2192 the pooled path activates. Leave it unset \u2192 plain `sqlite3.connect()`. Nothing else in the codebase branches on which one is active.\n\n**Connections are returned to the pool, not closed** \u2014 `putconn()`, not `.close()`. This matters at Cloud Run's request volume: opening a fresh Postgres connection per request would be measurably slower and could exhaust connection limits under concurrent load; pooling with `ThreadedConnectionPool(1, 10, ...)` caps it at 10 concurrent connections, reused across requests.\n\n---\n\n## Excel export\n\n`export_to_excel()` produces `output/job_tracker.xlsx` with three sheets:\n- **Applications** \u2014 every job, color-coded by status (`STATUS_COLORS` dict: gray=found, blue=applied, yellow=interviewing, green=offer, pink=rejected/ghosted) via `openpyxl`'s `PatternFill`\n- **Stats** \u2014 aggregate counts (via `get_stats()`)\n- **High Priority** \u2014 score \u2265 70, unapplied\n\nThis is the one place in the whole project that produces a file meant to be opened outside the dashboard entirely \u2014 useful if you want to review/filter jobs in a spreadsheet rather than the web UI.\n\n---\n\n## Status lifecycle\n\n```\nfound \u2192 applied \u2192 interviewing \u2192 offer\n                                \u2192 rejected\n                                \u2192 ghosted\n```\n\n`update_status()` is the single method every status transition goes through (single apply, batch apply, manual `main.py update` CLI command, and the dashboard's status-cycle button all call it) \u2014 it doesn't just flip the `jobs.status` column, it also appends a row to the `applications` table, which is what lets the dashboard's Analytics page reconstruct a real funnel (found \u2192 applied \u2192 interviewing \u2192 offer, with dates) rather than only ever seeing each job's *current* state.\n";
