export const learningDeepDiveMd = "# How the Learning > Playlists feature works \u2014 deep dive\n\nYou paste a YouTube playlist link into the dashboard. The app transcribes every video, turns each transcript into study notes, and lets you ask questions that get answered using only what's actually in those videos (not the AI's general knowledge). This is called **RAG** \u2014 Retrieval-Augmented Generation. Here's exactly how, end to end.\n\n---\n\n## The big picture\n\n```mermaid\nflowchart TD\n    A[\"You paste a playlist URL\\n(PlaylistPanel.tsx)\"] --> B[\"POST /api/learning/playlists/ingest\"]\n    B --> C[\"start_playlist()\\nlist videos via yt-dlp\\ncreate DB rows, status=pending\"]\n    C -->|\"returns immediately\"| D[\"Response: playlist_id\"]\n    B -.->|\"queued in background\"| E[\"process_playlist()\"]\n    E --> F[\"For each video:\\nget transcript\"]\n    F --> G[\"ask_ai(): summarize\\ntranscript into notes\"]\n    G --> H[\"chunk_text(): split into\\n~500-word overlapping pieces\"]\n    H --> I[\"ask_nvidia_embedding():\\nturn each chunk into a vector\"]\n    I --> J[\"FAISS index: store vectors\\nfor fast similarity search\"]\n    D --> K[\"Frontend polls\\nGET /playlists/{id} every 4s\"]\n    K --> L[\"Shows per-video status:\\npending -> transcribing ->\\nembedding -> done\"]\n    M[\"You type a question\"] --> N[\"POST /playlists/ask\"]\n    N --> O[\"Embed the question\"]\n    O --> P[\"Search FAISS for the\\n5 most similar chunks\"]\n    P --> Q[\"ask_ai(): answer using\\nONLY those chunks as context\"]\n    Q --> R[\"Answer + source video\\ntitles shown in UI\"]\n```\n\nTwo completely separate operations happen here: **ingest** (transcribe + index, slow, happens once per video) and **ask** (search + answer, fast, happens every time you ask a question). Let's go through each.\n\n---\n\n## Part 1: Ingest \u2014 turning a playlist into searchable knowledge\n\n### Step 1 \u2014 You paste a link (`web/components/PlaylistPanel.tsx`)\n\nThe `handleIngest` function calls `api.ingestPlaylist(url)`, which does:\n\n```ts\nfetch(`${API}/api/learning/playlists/ingest?url=...`, { method: 'POST' })\n```\n\n### Step 2 \u2014 The backend splits work into \"fast\" and \"slow\" (`app.py`)\n\n```python\n@app.post('/api/learning/playlists/ingest')\nasync def ingest_playlist(background_tasks: BackgroundTasks, url: str = Query(...)):\n    agent = StudyAgent()\n    playlist_id = await loop.run_in_executor(None, lambda: agent.start_playlist(url))\n    background_tasks.add_task(agent.process_playlist, playlist_id)\n    return {'ok': True, 'playlist_id': playlist_id}\n```\n\nThis is the key design decision: if the route waited for every video to be transcribed and embedded before responding, a 20-video playlist could hang the HTTP request for 10+ minutes and your browser would time out. Instead:\n\n- **`start_playlist()`** runs synchronously (but off the event loop, via `run_in_executor`, since it does blocking network calls) \u2014 it just asks yt-dlp to *list* the playlist's videos (no downloading), and writes one row per video into the database with `status = \"pending\"`. This takes a couple seconds.\n- **`process_playlist()`** is handed to FastAPI's `BackgroundTasks` \u2014 it keeps running *after* the response has already gone back to the browser. This is the slow part (transcribing + AI notes + embedding, potentially minutes per video).\n\nThe frontend gets `{ok: true, playlist_id}` back almost instantly, then starts polling for progress.\n\n### Step 3 \u2014 Listing the playlist without downloading (`agents/study_agent.py: _list_playlist_videos`)\n\n```python\nydl_opts = {\"extract_flat\": True, \"quiet\": True}\n```\n\n`extract_flat: True` tells yt-dlp \"just give me the list of video IDs/titles in this playlist, don't fetch full metadata or media for each one.\" This is why listing 50 videos takes 2 seconds, not 2 minutes.\n\n### Step 4 \u2014 Getting the transcript (`_get_transcript`)\n\nFor each video, there are two paths, tried in order:\n\n**Primary: YouTube's own captions.** Most videos have either creator-uploaded or auto-generated captions. The `youtube-transcript-api` library fetches these directly \u2014 no audio download, no AI, just reading text YouTube already has:\n\n```python\nsegments = YouTubeTranscriptApi().fetch(video_id)\ntext = \" \".join(s.text for s in segments)\n```\n\n**Fallback: Whisper.** If captions are disabled (`TranscriptsDisabled`) or don't exist (`NoTranscriptFound`), it falls back to `_whisper_transcribe`:\n\n```python\nydl.download([...])                    # yt-dlp downloads just the audio track\nmodel = whisper.load_model(\"base\")     # OpenAI's speech-to-text model, running locally\nresult = model.transcribe(audio_path)  # produces the transcript text\n```\n\nThis is why `ffmpeg` has to be installed on the machine \u2014 `yt-dlp`'s `FFmpegExtractAudio` postprocessor shells out to it to pull the audio stream out of the video and convert it to mp3. Whisper never touches the network for the AI part; the \"base\" model runs entirely on your CPU/GPU. It's slower and less accurate than captions, but it's the fallback of last resort, not the common path.\n\nIf *both* fail, the video's status becomes `failed` with an error message, and \u2014 this is the important bit \u2014 **the loop moves on to the next video**. One broken video in a playlist doesn't kill the other 19.\n\n**Update, found in production:** deployed on Cloud Run, every single video failed with `captions unavailable and Whisper fallback failed: ERROR: [youtube] ...: Sign in to confirm you're not a bot`. Reproducing the exact same call locally worked fine. Root cause: YouTube blocks known cloud-datacenter IP ranges (Google Cloud, AWS, etc.) from both the captions endpoint and yt-dlp's download, but not residential IPs \u2014 this is a YouTube-side anti-scraping measure, not a bug in this code. The fix is `YOUTUBE_COOKIES`: an optional env var holding a real logged-in browser session's cookies.txt contents, which both `youtube-transcript-api` and `yt-dlp` accept as proof of a real session:\n\n```python\ndef _cookies_file():\n    \"\"\"Writes YOUTUBE_COOKIES to a temp file once per process, returns its path.\"\"\"\n    raw = os.getenv(\"YOUTUBE_COOKIES\", \"\")\n    if not raw:\n        return None\n    fd, path = tempfile.mkstemp(suffix=\"_cookies.txt\")\n    with os.fdopen(fd, \"w\") as f:\n        f.write(raw)\n    return path\n```\n\nFor `youtube-transcript-api`, this is loaded into a `requests.Session` via `http.cookiejar.MozillaCookieJar` and passed to `YouTubeTranscriptApi(http_client=...)`. For yt-dlp, it's just `ydl_opts[\"cookiefile\"] = cookies_path` \u2014 yt-dlp's own documented mechanism for exactly this error. Completely opt-in: unset locally, the function returns `None` and nothing changes.\n\nWhile tracking this down, a second real bug turned up: the captions-fetch failure reason was being silently discarded (`except Exception: pass`) before falling through to Whisper, so the error message you'd see only ever showed Whisper's failure \u2014 never *why* captions failed in the first place. Fixed to carry both:\n\n```python\nexcept Exception as e:\n    captions_error = str(e)\n...\nreturn \"\", \"\", f\"captions failed ({captions_error}) and Whisper fallback failed: {e}\"\n```\n\nSmall lesson worth keeping in mind for any \"it works on my machine but not in prod\" bug: always check whether the deploy environment's network egress differs from your dev machine's before assuming the code itself changed behavior.\n\n### Step 5 \u2014 Turning a transcript into notes (`process_playlist`)\n\n```python\nnotes = ask_ai(\n    \"Turn this video transcript into structured study notes (markdown, \"\n    f\"headings + bullet points, grounded only in the transcript...):\\n\\n{text[:12000]}\",\n    max_tokens=1200,\n)\n```\n\n`ask_ai()` is the project's shared LLM entry point (`claude_client.py`) \u2014 it tries NVIDIA's free-tier chat model first, falls back to Gemini if that's unavailable, and falls back to a keyword heuristic if both AI providers are down. Same function every other AI-driven feature in the app uses (job scoring, cover letters, the interview trainer). The prompt explicitly says \"grounded only in the transcript\" to discourage the model from inventing facts not actually said in the video.\n\n### Step 6 \u2014 Chunking (`_chunk_text`)\n\nYou can't embed a 40-minute transcript as one vector \u2014 it's too long, and a single vector can't represent \"this video talks about five different subtopics\" precisely. So the transcript gets split into overlapping word-windows:\n\n```python\ndef _chunk_text(text, chunk_words=500, overlap_words=50):\n    words = text.split()\n    step = chunk_words - overlap_words   # = 450\n    for start in range(0, len(words), step):\n        chunk = \" \".join(words[start:start + chunk_words])\n```\n\nSo chunk 1 is words 0\u2013500, chunk 2 is words 450\u2013950, chunk 3 is words 900\u20131400, etc. The 50-word overlap exists so a sentence that happens to fall right on a chunk boundary doesn't get semantically cut in half and lose meaning in both halves.\n\nEach chunk is saved to the `learning_video_chunks` table with a UUID.\n\n### Step 7 \u2014 Embedding (`ask_nvidia_embedding`)\n\nAn **embedding** is a list of ~1024 numbers (a vector) that represents the *meaning* of a piece of text. Texts with similar meaning end up as vectors that point in similar directions in that 1024-dimensional space \u2014 this is what makes \"search by meaning, not exact keywords\" possible.\n\n```python\nvectors = ask_nvidia_embedding(chunks, input_type=\"passage\")\n```\n\nThis calls NVIDIA NIM's `bge-m3` model. Note `input_type=\"passage\"` \u2014 bge-m3 is an **asymmetric** model, meaning it embeds \"things you're indexing\" slightly differently than \"things you're searching with.\" When indexing chunks, you pass `\"passage\"`; when embedding a user's question later, you pass `\"query\"`. Using the wrong one doesn't crash anything, it just makes search results noticeably worse \u2014 it's a subtle but important detail bge-m3's docs call out.\n\n### Step 8 \u2014 Storing vectors in FAISS (`_add_to_index`)\n\nFAISS (Facebook AI Similarity Search) is a library for fast nearest-neighbor search over vectors \u2014 \"given this new vector, which of my 10,000 stored vectors are closest to it?\" without brute-force comparing against every single one naively (though at this project's scale, the index type used \u2014 `IndexFlatIP` \u2014 actually *is* brute-force, just a very fast, vectorized brute-force; more on that below).\n\n```python\ndef _add_to_index(chunk_ids, vectors):\n    index, order = _load_index()\n    arr = _normalize(np.array(vectors, dtype=\"float32\"))\n    index.add(arr)\n    order.extend(chunk_ids)\n    _save_index(index, order)\n```\n\nTwo files on disk hold all of this:\n- **`data/study_index.faiss`** \u2014 the actual FAISS index (just the vectors, in insertion order)\n- **`data/study_index_order.json`** \u2014 a plain list mapping *position in the index* \u2192 *chunk's database ID*\n\nWhy two files? FAISS's `IndexFlatIP` only knows about row positions (0, 1, 2, ...) \u2014 it has no concept of your UUIDs. So `order[7]` tells you \"the vector at position 7 in the FAISS index corresponds to chunk `f39d75b-...` in the database.\" This is how a FAISS search result (a position) gets turned back into an actual chunk of text with a video title attached.\n\n**`_normalize`** matters here too:\n\n```python\ndef _normalize(vecs):\n    norms = np.linalg.norm(vecs, axis=1, keepdims=True)\n    return vecs / norms\n```\n\n`IndexFlatIP` measures similarity via **inner product** (dot product), not cosine similarity directly. But if every vector is first scaled to length 1 (L2-normalized), inner product and cosine similarity become mathematically identical. So normalizing before storing \u2014 and normalizing the query the same way before searching \u2014 is what makes \"nearest neighbor by inner product\" actually mean \"most semantically similar,\" which is what you want.\n\n### Step 9 \u2014 Status tracking\n\nThroughout this whole pipeline, each video's row moves through statuses: `pending \u2192 transcribing \u2192 embedding \u2192 done` (or `failed` at any point, with an `error` message saved). The frontend polls `GET /api/learning/playlists/{id}` every 4 seconds and re-renders the status badge per video. Once every video is past the in-progress states, the poller clears its own interval \u2014 no more requests until you switch to a different playlist.\n\n```ts\nconst stillWorking = detail.videos?.some(\n  (v) => v.status === 'pending' || v.status === 'transcribing' || v.status === 'embedding'\n);\nif (!stillWorking && pollRef.current) clearInterval(pollRef.current);\n```\n\n---\n\n## Part 2: Ask \u2014 answering a question with RAG\n\nThis is the \"retrieval-augmented generation\" part. Retrieval (find relevant chunks) happens *before* generation (write an answer), and the generation step is forced to use only what retrieval found.\n\n### Step 1 \u2014 Embed the question\n\n```python\nvectors = ask_nvidia_embedding([question], input_type=\"query\")\n```\n\nSame `bge-m3` model, but `input_type=\"query\"` this time (see Step 7 above \u2014 asymmetric embedding).\n\n### Step 2 \u2014 Search FAISS for similar chunks\n\n```python\nraw_results = _search_index(vectors[0], k=k * 4 if playlist_id else k)\n```\n\n```python\ndef _search_index(query_vector, k=5):\n    index, order = _load_index()\n    q = _normalize(np.array([query_vector]))\n    scores, positions = index.search(q, min(k, index.ntotal))\n    return [(order[pos], score) for pos, score in zip(positions[0], scores[0])]\n```\n\nNotice `k * 4` when a specific `playlist_id` was given. This is because the FAISS index is **global** \u2014 it holds chunks from every playlist you've ever ingested, not filtered by playlist. So if you're asking a question scoped to one playlist, the code over-fetches (20 candidates instead of 5) and then filters down to the ones that actually belong to your chosen playlist:\n\n```python\nif playlist_id:\n    chunk_ids = [cid for cid in chunk_ids if chunk_map[cid][\"playlist_id\"] == playlist_id][:k]\n```\n\nThis is a deliberate simplification: a \"real\" production RAG system might maintain a separate FAISS index per playlist, or use a vector DB with native metadata filtering (Pinecone, Qdrant, pgvector). Here, one combined index + over-fetch-and-filter is simpler code and fine at this project's scale (dozens of playlists, not millions of chunks) \u2014 the trade-off is it wastes some search work when you scope to a specific playlist.\n\n### Step 3 \u2014 Look up the actual text\n\nFAISS gives back chunk *IDs*, not the text itself (FAISS only stores vectors). So there's a DB lookup to turn IDs into the real transcript excerpts and their video titles:\n\n```python\nchunk_map = self.tracker.get_chunks_by_ids(candidate_ids)\n```\n\n### Step 4 \u2014 Build a grounded prompt and generate the answer\n\n```python\ncontext = \"\\n\\n\".join(f\"[{c['video_title']}]: {c['text']}\" for ...)\nprompt = (\n    \"Answer the question using ONLY the context below. If the answer isn't \"\n    f\"in the context, say so.\\n\\nContext:\\n{context}\\n\\nQuestion: {question}\"\n)\nanswer = ask_ai(prompt, max_tokens=600)\n```\n\nThis is the whole trick of RAG in one sentence: instead of asking the LLM \"what do you know about X\" (which risks it confidently making things up \u2014 a \"hallucination\"), you hand it the *actual relevant excerpts* and say \"answer using only this.\" The model is still doing the writing, but it's constrained to synthesizing from real material you retrieved, and it's explicitly told to say \"not in the context\" rather than guess.\n\nThe response also returns which video titles the answer's chunks came from (`sources`), which is why you see \"Sources: ...\" under the answer in the UI \u2014 that's not the AI naming sources from memory, it's literally the video titles of the chunks that got retrieved.\n\n---\n\n## Database schema\n\nThree tables, all in `agents/tracker.py`:\n\n```\nlearning_playlists\n\u251c\u2500\u2500 id (UUID)\n\u251c\u2500\u2500 url\n\u251c\u2500\u2500 title\n\u251c\u2500\u2500 status          \"ingesting\" -> \"done\"\n\u2514\u2500\u2500 created_at\n\nlearning_videos\n\u251c\u2500\u2500 id (UUID)\n\u251c\u2500\u2500 playlist_id     \u2192 learning_playlists.id\n\u251c\u2500\u2500 video_id        (YouTube's own ID, e.g. \"dQw4w9WgXcQ\")\n\u251c\u2500\u2500 title, url\n\u251c\u2500\u2500 transcript       (full text)\n\u251c\u2500\u2500 notes_md         (AI-generated study notes)\n\u251c\u2500\u2500 source            \"captions\" | \"whisper\"\n\u251c\u2500\u2500 status            pending -> transcribing -> embedding -> done | failed\n\u251c\u2500\u2500 error\n\u2514\u2500\u2500 created_at\n\nlearning_video_chunks\n\u251c\u2500\u2500 id (UUID)                \u2190 this is what FAISS's order.json points to\n\u251c\u2500\u2500 video_id        \u2192 learning_videos.id\n\u251c\u2500\u2500 playlist_id                (denormalized, so `ask()` can filter by playlist\n\u2502                                 without a join through learning_videos)\n\u251c\u2500\u2500 chunk_index\n\u251c\u2500\u2500 text\n\u2514\u2500\u2500 created_at\n```\n\nWhy is `playlist_id` duplicated on the chunks table when it could be derived by joining through `video_id \u2192 learning_videos.playlist_id`? Because the `ask()` flow needs to filter thousands of retrieved chunks down to \"just this playlist's\" as fast as possible, and a JOIN for every question would be more code and a slower query than a plain column already sitting on the row. Small deliberate denormalization, not an oversight.\n\n---\n\n## The frontend piece (`web/components/PlaylistPanel.tsx`)\n\nThree pieces of state drive the whole UI:\n\n1. **`playlists`** \u2014 the sidebar list, loaded once via `api.listPlaylists()` on mount.\n2. **`selected`** \u2014 whichever playlist is open in the main panel; polled every 4s while any of its videos are still in progress (see Part 1, Step 9).\n3. **`answer`** \u2014 set only after you ask a question; cleared each time you ask a new one.\n\nThe \"Ask a question\" box calls `api.askPlaylists(question, selected?.id)` \u2014 note it always scopes to whichever playlist is currently open, so asking a question always searches that playlist specifically, not your entire library (unless you were to call the API without a `playlist_id`, which the UI doesn't currently expose \u2014 global search is technically possible via the same endpoint but the frontend never triggers it that way).\n\n---\n\n## Where each piece lives\n\n| What | File |\n|---|---|\n| Ingest orchestration, transcript fetch, RAG `ask()` | `agents/study_agent.py` |\n| Chat/embedding calls (shared with rest of app) | `claude_client.py` |\n| DB schema + CRUD for playlists/videos/chunks | `agents/tracker.py` |\n| HTTP routes | `app.py` (search `/api/learning/playlists`) |\n| Frontend API client (typed fetch wrappers) | `web/lib/api.ts` |\n| Playlists tab UI | `web/components/PlaylistPanel.tsx` |\n| Tab wiring | `web/app/learning/page.tsx` |\n\n## Known simplifications (deliberate, not bugs)\n\n- **One global FAISS index for all playlists**, filtered by over-fetching \u2014 see Part 2, Step 2. Fine at this scale; would need a rethink at real scale (thousands of playlists).\n- **`BackgroundTasks` instead of a real job queue** (Celery/RQ) \u2014 if the Cloud Run/uvicorn process restarts mid-ingest, an in-progress playlist gets stuck at whatever status it was last at, with no automatic retry. Acceptable for a single-user tool; would matter for anything multi-tenant.\n- **Whisper's \"base\" model**, not one of the larger/more accurate ones \u2014 a deliberate speed/accuracy trade-off since it's a fallback path, not the common case.\n";
