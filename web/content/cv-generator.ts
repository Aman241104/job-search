export const cvGeneratorMd = "# CV & cover letter generator \u2014 how a tailored application gets written\n\n`agents/cv_customizer.py`, `CVCustomizerAgent`. Runs whenever you apply to a job (single or batch) \u2014 one call produces both documents.\n\n---\n\n## The pipeline\n\n```mermaid\nflowchart TD\n    A[\"prepare_full_package(job)\"] --> B[\"_select_relevant_projects()\\nkeyword-overlap scoring,\\nno AI call\"]\n    B --> C[\"customize_for_job():\\nask_ai() writes tailored\\nMarkdown resume\"]\n    B --> D[\"generate_cover_letter():\\nask_ai() writes 3-paragraph\\ncover letter\"]\n    C --> E[\"_refine(): second ask_ai()\\nself-critique pass\"]\n    D --> E\n    E --> F[\"_markdown_to_pdf():\\nWeasyPrint renders to PDF\"]\n    F --> G[\"output/cv_{job_id}.pdf\\noutput/cover_{job_id}.pdf\"]\n```\n\n## Picking which projects to feature \u2014 no AI needed\n\nBefore writing anything, `_select_relevant_projects()` picks the best 3-5 projects from `data/projects_detailed.json` for *this specific job*, using plain keyword overlap:\n\n```python\ndef _select_relevant_projects(self, job, top_n=5):\n    text = (job['title'] + ' ' + job['description']).lower()\n    scored = []\n    for p in self.projects_detailed[\"flagship\"]:\n        overlap = sum(1 for kw in p.get(\"keywords\", []) if kw.lower() in text)\n        tier_bonus = {\"S\": 2, \"A\": 1, \"C\": 0}.get(p.get(\"tier\", \"C\"), 0)\n        scored.append((overlap + tier_bonus, p))\n    scored.sort(key=lambda x: x[0], reverse=True)\n    return [p for _, p in scored[:top_n]] + [self.projects_detailed[\"gallery\"]]\n```\n\nEach project in the JSON has a `keywords` list and a `tier` (\"S\"/\"A\"/\"C\", how flagship it is). The score is just \"how many of this project's keywords appear in the job posting\" plus a small tier tiebreaker \u2014 same cheap, deterministic, no-API-call pattern `job_finder.py`'s keyword scorer uses. Only the resulting shortlist (not the full project library) is handed to the AI prompt, so the model can't invent a project that wasn't already pre-selected as relevant.\n\n## Writing the resume\n\n`customize_for_job()` builds one large prompt containing: the full master resume JSON (`data/master_resume.json`), the pre-selected projects JSON, and the job's title/company/location/description. The instructions are explicit about what NOT to do \u2014 this is the load-bearing part:\n\n- \"Do NOT invent functionality not listed\" (for project bullets)\n- \"Do NOT add fake experience or skills the candidate doesn't have\"\n- \"Avoid generic filler language ('eager to collaborate on cutting-edge projects'...) \u2014 keep the summary specific and grounded\"\n- \"Do NOT bolt on generic justification clauses that just restate a JD phrase\"\n\nThese read like they're compensating for specific bad outputs seen in practice, not defensive boilerplate \u2014 a fresh model call tends to drift toward exactly this kind of generic, JD-mirroring language unless explicitly told not to.\n\n## The self-critique pass \u2014 `_refine()`\n\nBoth the resume and cover letter go through a second AI call after the first draft:\n\n```python\ndef _refine(self, draft, job, doc_type, extra_checks, max_tokens):\n    prompt = f\"\"\"Review this draft {doc_type} against the target job below.\nIf you find real issues, rewrite it. If it's already good, return it unchanged...\nCheck for: Generic filler language..., Invented achievements..., Weak keyword alignment..., {extra_checks}\"\"\"\n    refined = ask_ai(prompt, max_tokens=max_tokens)\n    return refined if refined else draft\n```\n\nThis is a genuine quality pass, not decoration \u2014 the prompt explicitly permits \"return it unchanged\" so the model isn't incentivized to rewrite for the sake of rewriting. `extra_checks` is doc-specific: the resume pass also checks ATS-friendliness (no tables/columns, standard section headers); the cover letter pass checks the 3-paragraph structure and that no greeting/sign-off/placeholder brackets snuck in. If this second call fails for any reason, `_refine` just returns the original first draft \u2014 the two-call design is additive, never a hard dependency.\n\n## Why WeasyPrint, not a browser\n\n```python\n# WeasyPrint instead of a full browser (Playwright/Chromium) \u2014 a headless\n# Chromium launch was measured at ~591MB RSS for a single PDF render,\n# already over Render's entire 512MB free-tier limit on its own (confirmed\n# via a real OOM crash in production).\n```\n\n`_markdown_to_pdf()` converts the AI's Markdown output to HTML (via the `markdown` package) and renders straight to PDF with WeasyPrint \u2014 no browser process at all. This was a real production incident, not a hypothetical: a headless Chromium launch alone exceeded the entire memory budget of the free-tier host at the time. Worth remembering as a general pattern: rendering a PDF from a document this simple (no JS, no complex layout) never actually needed a browser in the first place.\n\n## The cover letter's PDF gets its own greeting/sign-off\n\n`generate_cover_letter()`'s AI output is deliberately just the body paragraphs \u2014 no \"Dear Hiring Team\" or sign-off, because `job_applier.py`'s emailed version wraps that around the body itself for the *email*. But that wrap never touched the standalone PDF file \u2014 `save_cover_letter()` re-adds the same wrap (`Dear Hiring Team, ... Best regards, {name}...`) specifically for the PDF, so someone opening the attached file directly (not reading the email body) still sees a complete letter, not a bare paragraph. Small detail, but the kind of thing that's invisible until someone actually opens the attachment.\n";
