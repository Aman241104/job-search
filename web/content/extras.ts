export const extrasMd = "# Legitimacy check, contact discovery, story bank, and the book library\n\nFour smaller features that don't each warrant a full doc on their own, grouped here. All four are examples of the same underlying pattern: a focused `ask_ai()` prompt with an explicit rubric, wrapped around a small, honest scope \u2014 none of them pretend to do more than they actually can.\n\n---\n\n## Legitimacy check \u2014 `check_legitimacy()` in `claude_client.py`\n\nAnswers \"does this posting look like a real, well-specified opening, or generic/low-effort/potentially fake?\" \u2014 purely from the job's own text and metadata already in the database, no live re-visit of the posting.\n\n```python\ndef check_legitimacy(job: dict) -> dict:\n    prompt = f\"\"\"Analyze this job posting for signals of legitimacy...\n    Check for (add a flag only when the signal is actually present, don't force-fit unrelated ones):\n    - Generic boilerplate ratio (JD reads like a template vs describes real day-to-day work)\n    - Specific technologies/tools named vs vague buzzwords only\n    - Realistic experience requirements (years asked vs how long the named tech has existed)\n    - Internal contradictions (e.g. entry-level title paired with staff/lead-level asks)\n    - Salary/compensation mentioned or not\n    - Clear scope for the role vs a vague catch-all description\n    \"\"\"\n    ...\n    return {\"score\": int, \"flags\": [str, ...]}\n```\n\nThe flags are deliberately framed as *neutral observations*, not accusations \u2014 a posting can legitimately be missing salary info without being fake. `GET /api/jobs/{job_id}/legitimacy` in `app.py` calls this on demand (not eagerly on every scraped job \u2014 it's a per-job AI call, so it only runs when you actually open a job's detail view and ask). If the AI call fails entirely, `score` comes back `None` rather than a fake default number, so the UI can distinguish \"checked, looks fine\" from \"couldn't check.\"\n\n---\n\n## Contact discovery \u2014 `contact_finder.py`\n\nExplicitly scoped down from what it might sound like:\n\n```python\n\"\"\"Contact discovery \u2014 inspired by career-ops's \"contacto\" mode, scoped down\nto what's actually achievable: we can't scrape LinkedIn directly (already a\ndocumented dead end for job boards, bot-blocked same as Naukri/Indeed), so\nthis is a search-assist + message-draft tool rather than automated contact\nscraping. The user still has to find and pick the real contact by hand.\n\"\"\"\n```\n\n`draft_contact_outreach(job)` returns three things, none of which involve actually finding a person automatically:\n\n1. **`search_query`** \u2014 a pre-built Google `site:linkedin.com/in` search string (company name + recruiter/hiring-manager/engineering-manager keywords)\n2. **`search_url`** \u2014 that query, already URL-encoded into a clickable Google search link\n3. **`message_draft`** \u2014 an AI-written, hard-capped 300-character LinkedIn connection message, referencing one specific real project (reusing `CVCustomizerAgent._select_relevant_projects()` rather than duplicating that logic)\n\nYou still open the search link, look at the results, and decide who to actually message \u2014 this tool only removes the \"what do I search for\" and \"what do I say\" steps, not the \"who is the right person\" step, which genuinely can't be automated reliably without scraping LinkedIn (already ruled out \u2014 see `job_finder.py`'s LinkedIn comments on IP-flagging risk).\n\n---\n\n## Story Bank \u2014 STAR+Reflection interview stories\n\n`draft_star_story()` in `claude_client.py`, backed by the `story_bank` table. You paste in rough, informal notes about something you did (\"built X, ran into Y, fixed it by Z\") and it gets structured into a proper interview story:\n\n```python\n{\"situation\": \"...\", \"task\": \"...\", \"action\": \"...\", \"result\": \"...\", \"reflection\": \"...\", \"tags\": [...]}\n```\n\nThe `reflection` field \u2014 \"what was learned or what would be done differently\" \u2014 is called out explicitly in the prompt as the one that separates a senior-sounding answer from a junior one:\n\n```python\n- Reflection: what was learned or what would be done differently \u2014 this is what signals seniority, don't skip it\n```\n\nSame \"don't invent\" discipline as the CV generator: \"Use ONLY details present in the rough notes \u2014 do not invent facts, numbers, or outcomes not mentioned.\" `tags` are 2-4 short competency labels (leadership, conflict resolution, ownership, etc.) generated alongside the story, used for filtering in the Story Bank UI. Stories are editable after drafting (`PUT /api/stories/{id}`) \u2014 the AI draft is a starting point, not a final answer you're stuck with.\n\n---\n\n## Book library \u2014 upload a PDF, get a page-by-page AI tutor\n\n`POST /api/learning/books/upload` in `app.py`. Two things happen to an uploaded PDF:\n\n1. **Text extraction, with an OCR fallback.** `pypdf` extracts text per page first (fast, works for any text-based PDF). Any page that comes back empty (scanned/image-only page) gets a second pass: `pdf2image` rasterizes just that page to an image, then `pytesseract` (Tesseract OCR) reads it. One page's OCR failing doesn't fail the upload \u2014 that page just stays empty, the rest of the book is still usable.\n2. **Storage split two ways.** Extracted text goes straight into Postgres/SQLite (via `TrackerAgent.add_book()`) \u2014 this is what reading, page summaries, and the per-book chat all depend on. The *raw PDF bytes* separately go to Cloudinary (`upload_pdf()`), purely so there's a \"download the original file\" link \u2014 Cloud Run's local disk doesn't survive a redeploy, so without this the uploaded file would just vanish on the next deploy. This upload is explicitly best-effort: if Cloudinary isn't configured or the call fails, you just don't get a download link \u2014 reading/summarizing/chatting about the book never depends on it succeeding, since those all read the already-extracted text from the database, not the Cloudinary file.\n\nWhy not just always render every page to an image and OCR it? Because most real PDFs (typed books, not scans) already have selectable text \u2014 `pypdf` extracting it directly is both faster and more accurate than OCR would be; OCR is reserved for the actual scanned-page case where there's no text layer to extract at all.\n";
