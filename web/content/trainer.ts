export const trainerMd = "# Interview trainer \u2014 how the mock interview coach works\n\n`agents/trainer.py`, `TrainerAgent`. Runs via `python main.py train` (CLI) or the dashboard's Train page.\n\n---\n\n## 8 topics, each its own system prompt\n\n```python\nTRAINING_TOPICS = {\n    \"1\": (\"Technical - React/JS/TS\", \"react_js\"),\n    \"2\": (\"Technical - Node.js/Backend\", \"backend\"),\n    \"3\": (\"DSA - Arrays & Strings\", \"dsa_arrays\"),\n    \"4\": (\"DSA - Trees & Graphs\", \"dsa_trees\"),\n    \"5\": (\"System Design Basics\", \"system_design\"),\n    \"6\": (\"HR & Behavioral (STAR method)\", \"behavioral\"),\n    \"7\": (\"Portfolio Walkthrough Practice\", \"portfolio\"),\n    \"8\": (\"Salary Negotiation\", \"salary\"),\n}\n```\n\nEach maps to a fixed, hand-written `SYSTEM_PROMPTS[topic_id]` string that sets the interviewer persona and what to cover. Nothing dynamic here \u2014 the prompts are static, tuned once (e.g. the `\"portfolio\"` prompt lists the candidate's actual real projects by name so the AI interviewer asks about things that actually exist, not generic portfolio questions). If a specific job is being practiced for, `run_session(job=...)` appends one extra line: `\"This interview is for: {title} at {company}. Tailor questions to this role.\"`\n\n## The conversation loop\n\n```python\nchat = GeminiChat(system=system_prompt, temperature=0.8)\nfirst_q = chat.send(\"Start the interview. Greet me briefly and ask your first question.\")\n...\nwhile question_count < max_questions:  # max_questions = 8\n    user_input = input(...)\n    feedback = chat.send(\n        f\"{user_input}\\n\\n[Score my answer /10, give specific feedback, \"\n        f\"provide the model answer, then ask the next question.{suffix}]\"\n    )\n    score_match = re.search(r'(\\d+)\\s*/\\s*10', feedback)\n    if score_match:\n        scores.append(int(score_match.group(1)))\n```\n\n`GeminiChat` (in `claude_client.py`) is a stateful multi-turn wrapper \u2014 despite the name, it tries NVIDIA first each turn and only falls back to Gemini if that's unavailable, keeping conversation history in OpenAI's `role: user/assistant` format (converted to Gemini's `role: user/model` shape only on the fallback path). Every turn is really just one instruction embedded in the user message: \"score this, explain, ask the next one\" \u2014 there's no separate scoring call, the same response that gives feedback also asks the next question, in one model turn.\n\nScores are extracted from the model's own text via a regex looking for `N/10` \u2014 not a structured JSON response. This is a soft coupling: it works because the system prompt explicitly asks for a `/10` score in each response, but a model that phrased it differently (e.g. \"8 out of 10\") wouldn't be caught. Simple, and good enough given the model reliably follows the format when told to in the system prompt.\n\n## What gets saved\n\nAt session end, if at least one question was answered:\n\n```python\nself._save_session(topic_name, scores, weak_areas)\n```\n\nPersisted to `data/training_log.json` (a flat JSON file \u2014 not the SQLite/Postgres `training_sessions` table `tracker.py` also defines; the CLI path and the file used by `main.py train --progress` are the JSON log, while the dashboard's persisted history goes through the DB table separately). `weak_areas` collects any question scored below 6/10, tagged by question number \u2014 this is what `python main.py train --progress` surfaces as \"areas to focus on\" across your last 10 sessions.\n\n## Skip and quit\n\nTyping `skip` doesn't just move on silently \u2014 it substitutes a specific message (\"I'm not confident about this one \u2014 please show me the model answer and move on\") so the interviewer still explains the correct answer before advancing, rather than the question just vanishing with no learning value. `quit`/`q`/`exit` ends the session early; whatever questions were answered before that still get scored and saved.\n";
