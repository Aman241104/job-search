export const architectureMd = "# System architecture \u2014 how everything connects\n\nThis is the map before the deep dives into individual features. If you're new to this codebase, read this one first.\n\n---\n\n## The shape of it\n\n```mermaid\nflowchart LR\n    U[\"You\"] --> FE[\"Next.js frontend\\n(web/) \u2014 Vercel\"]\n    FE -->|\"fetch, NEXT_PUBLIC_API_URL\"| BE[\"FastAPI backend\\n(app.py) \u2014 Cloud Run\"]\n    BE --> DB[(\"Postgres\\n(Supabase) or\\nlocal SQLite\")]\n    BE --> AI1[\"NVIDIA NIM\\n(primary AI)\"]\n    BE --> AI2[\"Gemini\\n(fallback AI)\"]\n    BE --> SCRAPE[\"~14 job sources\\n(scrapers + free APIs)\"]\n    BE --> YT[\"yt-dlp / Whisper /\\nyoutube-transcript-api\"]\n    BE --> STORE[\"Cloudinary\\n(uploaded PDFs)\"]\n```\n\nTwo deployables, one shared database:\n\n| Piece | Where it lives | Deployed to |\n|---|---|---|\n| Frontend | `web/` (Next.js 14, App Router) | Vercel |\n| Backend | everything at the repo root + `agents/` | Google Cloud Run (Docker) |\n| Database | Postgres (Supabase) in production, SQLite locally | Supabase / local `data/applications.db` |\n\nThere's also a CLI (`main.py`) that talks to the exact same `agents/*.py` classes the FastAPI routes call \u2014 the backend isn't a separate implementation, `app.py` is just an HTTP wrapper around the same orchestrator/agent classes `main.py` drives directly. See \"Two entry points, one core\" below.\n\n---\n\n## The AI provider chain \u2014 `claude_client.py`\n\nEvery AI call in the entire project (scoring, CV writing, interview coaching, RAG notes/answers, legitimacy checks, story drafting) funnels through one function:\n\n```python\ndef ask_ai(prompt, system=\"\", max_tokens=1000, temperature=0.7):\n    if AI_PROVIDER == \"claude_code\":\n        result = ask_claude_code(prompt, system=system)\n        if result: return result\n    result = ask_nvidia(prompt, ...)\n    if result: return result\n    return ask_gemini(prompt, ..., quality_first=True)\n```\n\nThe chain, in order:\n\n1. **Claude Code CLI** (`ask_claude_code`) \u2014 only if `AI_PROVIDER=claude_code` is explicitly set. Shells out to your local `claude -p` command, using your existing `claude login` session (subscription usage, not metered API billing). Only works on a machine with the CLI installed and logged in \u2014 deliberately never set in production, since it just fails silently there and falls through.\n2. **NVIDIA NIM** (`ask_nvidia`) \u2014 the real primary provider. Free tier, ~40 requests/minute shared across models, which is why this took over from Gemini as the default: Gemini's smart model only allows 20 calls/day.\n3. **Gemini** (`ask_gemini`) \u2014 fallback if NVIDIA is unset or rate-limited. Tracks per-model cooldowns to disk (`data/gemini_cooldowns.json`) so a restart doesn't forget a model was already rate-limited today.\n4. **Keyword fallback** (`_keyword_fallback`) \u2014 zero-AI, inside `ask_gemini`. If the prompt looks like a job-scoring request, returns a crude keyword-based score so the pipeline never fully breaks even with no AI providers configured at all.\n\nEvery step **fails open, never closed** \u2014 a missing key or a 429 just returns `\"\"` and the caller moves to the next provider. Nothing in this chain ever raises and crashes a caller.\n\nThe same module also exposes `ask_nvidia_embedding()` (NVIDIA's `bge-m3` embedding model, used only by the Study RAG feature \u2014 see that doc) and `GeminiChat` (a stateful multi-turn wrapper used by the interview trainer, despite the name it tries NVIDIA first too).\n\n---\n\n## The database \u2014 dual SQLite/Postgres, one codebase\n\n`agents/tracker.py` is the single source of truth for all persisted data (17 tables: jobs, applications, training sessions, blacklisted companies, interview rounds, learning items/topics/books/pages, story bank, settings, batches/batch_items, learning playlists/videos/chunks, telegram alerts).\n\nThe trick that makes this work identically in both environments:\n\n```python\nDATABASE_URL = os.getenv(\"DATABASE_URL\", \"\")\n_pg_pool = None\nif DATABASE_URL:\n    _pg_pool = psycopg2.pool.ThreadedConnectionPool(1, 10, DATABASE_URL)\n\ndef _get_conn(self):\n    if _pg_pool:\n        return _PgConnWrapper(_pg_pool)\n    return sqlite3.connect(self.db_path)\n```\n\n`_PgConnWrapper` is a drop-in stand-in for a `sqlite3.Connection` \u2014 it implements the same `.execute()` / `.row_factory` interface, silently rewriting `?`-style placeholders to Postgres's `%s`, and wrapping cursor results in dicts the same way `sqlite3.Row` does. Every one of the ~50 query methods in `tracker.py` (and every direct `tracker._get_conn()` call in `app.py`) was written once, against the SQLite API, and works unmodified against Postgres too. Set `DATABASE_URL` \u2192 Postgres. Leave it unset \u2192 SQLite. Nothing else in the codebase needs to know which one is active.\n\nWhy this matters practically: local dev needs **zero setup** (SQLite file auto-creates on first run), while production gets a real concurrent-safe database, from the same code.\n\n---\n\n## Two entry points, one core\n\n```mermaid\nflowchart TD\n    CLI[\"main.py (CLI)\"] --> ORCH[\"orchestrator.py\\nJobSearchOrchestrator\"]\n    API[\"app.py (FastAPI)\"] --> AGENTS\n    ORCH --> AGENTS[\"agents/*.py\\n(JobFinderAgent, CVCustomizerAgent,\\nTrackerAgent, TrainerAgent,\\nJobApplierAgent, StudyAgent, ...)\"]\n```\n\n`orchestrator.py`'s `JobSearchOrchestrator` is what `main.py`'s CLI commands (`find`, `apply`, `track`, `train`) call \u2014 it just instantiates all five original agents and wires simple methods like `daily_run()` and `apply_to_top_jobs()`. `app.py`'s HTTP routes mostly skip the orchestrator and call the agent classes directly (since the web dashboard needs finer-grained control than the CLI's all-or-nothing commands), but it's the exact same underlying `agents/*.py` classes either way \u2014 nothing is reimplemented for the web version.\n\n---\n\n## Every agent, one line each\n\n| Agent | File | Job |\n|---|---|---|\n| JobFinderAgent | `agents/job_finder.py` | Scrapes ~14 job sources, dedupes, scores with keyword+AI hybrid |\n| CVCustomizerAgent | `agents/cv_customizer.py` | Tailors resume + writes cover letter per job, renders to PDF |\n| TrackerAgent | `agents/tracker.py` | All persistence \u2014 17-table schema, dual SQLite/Postgres, Excel export |\n| TrainerAgent | `agents/trainer.py` | Multi-turn interview coach, 8 topics, scored /10 per answer |\n| JobApplierAgent | `agents/job_applier.py` | Single-job apply flow: browser open, or SMTP email with PDF attachments |\n| StudyAgent | `agents/study_agent.py` | YouTube playlist RAG \u2014 see the dedicated deep dive |\n| batch_applier.py | (module, not a class) | Batch apply across email / Telegram / browser-prefill channels |\n| contact_finder.py | (module) | Drafts a LinkedIn search query + outreach message per job |\n| telegram_notifier.py | `agents/telegram_notifier.py` | Pushes job alerts to Telegram, handles inbound \"applied\" replies |\n| cloudinary_storage.py | `agents/cloudinary_storage.py` | Persists uploaded book/PDF files across deploys (Cloud Run's disk is ephemeral) |\n\nEach has its own deep-dive doc \u2014 see `/docs`.\n\n---\n\n## Deployment topology\n\n```mermaid\nflowchart LR\n    GH[\"GitHub\\n(Aman241104/job-search)\"] -->|\"git push\"| V[\"Vercel\\n(frontend, web/)\"]\n    GH -.->|\"gcloud run deploy\\n(manual, not auto)\"| CR[\"Cloud Run\\n(backend, job-serach-api)\"]\n    CR --> SB[(\"Supabase Postgres\")]\n```\n\nImportant asymmetry: the Vercel side is CLI-linked to this GitHub repo but a push doesn't currently auto-trigger a build (no GitHub integration webhook wired up on the Vercel project side) \u2014 deploys are `vercel deploy --prod` run explicitly. The Cloud Run backend is *never* auto-deployed from git at all \u2014 it's `gcloud run deploy --source .`, a manual build-and-push from whatever's checked out locally at the time. Both of these are worth knowing if something \"should have updated but didn't.\"\n\n---\n\n## Config \u2014 one file, `config.py`\n\nEvery environment variable the whole project reads is centralized here (loaded via `python-dotenv`'s `load_dotenv()`), with inline comments documenting *why* each default is what it is \u2014 worth skimming directly rather than summarizing, since the comments there carry real operational history (which NVIDIA models were verified working, why `OUTPUT_DIR` has to be created eagerly, etc). `USER_PROFILE`, `JOB_PREFERENCES`, and `LEARNING_TRACK` also live here as plain Python dicts/lists \u2014 no admin UI for these, you edit the file directly.\n";
